# Benchmark records

Commit small, sanitized benchmark summaries here when they are used for architecture or routing decisions. Raw task content, RPC logs, API responses and credentials remain local under `storage/`.

Each committed summary should record date, dataset version, provider/model, completion and correctness, manual revision rate, P50/P95 duration, peak memory, token totals and reported cost.
