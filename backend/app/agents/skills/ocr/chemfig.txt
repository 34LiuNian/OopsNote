当题目涉及有机结构式或标注了 \chemfig 时，请遵循以下规则：

- 输出的 LaTeX 必须使用 \chemfig{...} 包裹结构式。
- 结构式应放入 latex_blocks 中，保持原样，不要擅自改写。
- 题干里的化学式如果可用 mhchem 表达，可保留在题干；结构式单独放到 latex_blocks。
- 遇到无法识别的结构式时，保留原始 OCR 文本到 ocr_text，避免编造。
